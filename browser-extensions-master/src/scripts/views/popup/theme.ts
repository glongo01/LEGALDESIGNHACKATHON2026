// theme.ts — header color is applied via CSS classes in service.ts
// remote logo fetching removed; all data is now bundled locally
export async function applyHeaderTheme(_serviceId: string): Promise<void> { /* no-op */ }
export async function applyHeaderColor(_rating: string): Promise<void> { /* no-op */ }
